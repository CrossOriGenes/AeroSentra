<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="15dp"
    android:background="@drawable/card_gradient_bg"
    android:backgroundTint="#225864"
    android:orientation="vertical"
    android:paddingHorizontal="5dp"
    android:paddingVertical="10dp">

    <!-- HEADER -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:weightSum="2">

        <TextView
            android:layout_width="175dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/jost_extrabold"
            android:paddingStart="8dp"
            android:paddingEnd="0dp"
            android:text="@string/content_3_title"
            android:textColor="#B8B7B7"
            android:textSize="20sp" />
        <!-- TOGGLE NAV -->
        <com.google.android.material.button.MaterialButtonToggleGroup
            android:id="@+id/minitabToggler1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:checkedButton="@id/btnHourly"
            app:selectionRequired="true"
            app:singleSelection="true">
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnHourly"
                style="@style/ToggleButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/content_3_tab_label2" />
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDaily"
                style="@style/ToggleButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/content_3_tab_label1" />
        </com.google.android.material.button.MaterialButtonToggleGroup>
    </LinearLayout>

    <!-- CHART -->
    <HorizontalScrollView
        android:id="@+id/chartWrapper"
        android:layout_width="match_parent"
        android:layout_height="260dp"
        android:layout_marginBottom="5dp"
        android:background="@android:color/transparent"
        android:fillViewport="true"
        android:paddingStart="5dp"
        android:paddingTop="5dp"
        android:paddingEnd="5dp"
        android:paddingBottom="0dp"
        android:contentDescription="@string/content_3_title">
        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/lineChartForTemperature"
            android:layout_width="600dp"
            android:layout_height="250dp" />
    </HorizontalScrollView>

</LinearLayout>