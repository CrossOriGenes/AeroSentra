<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="210dp"
    android:background="@drawable/card_gradient_bg"
    android:elevation="5dp"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/dashboard_header"
    android:translationY="30dp"
    android:layout_marginHorizontal="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:paddingStart="15dp"
        android:paddingTop="10dp"
        android:paddingEnd="10dp"
        android:paddingBottom="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="190dp"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="vertical"
            android:paddingStart="5dp"
            android:paddingEnd="0dp">
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/jost_extrabold"
                android:text="@string/card1_title"
                android:textColor="@color/black"
                android:textSize="30sp" />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:layout_marginBottom="30dp"
                android:fontFamily="@font/jost_medium"
                android:lineSpacingExtra="-6dp"
                android:text="@string/card1_description"
                android:textColor="#888"
                android:textSize="14sp" />
            <Button
                android:id="@+id/goToTriggerRoverActivityBtn"
                android:layout_width="150dp"
                android:layout_height="wrap_content"
                android:backgroundTint="@color/black"
                android:fontFamily="@font/jost_semibold"
                android:text="@string/card1_btn"
                android:textColor="@color/white"
                android:textColorLink="@color/white"
                android:textSize="15sp"
                app:rippleColor="#333333" />
        </LinearLayout>

        <ImageView
            android:id="@+id/roverIcon"
            android:layout_width="130dp"
            android:layout_height="160dp"
            android:alpha="0.6"
            android:contentDescription="@string/card1_title"
            android:src="@drawable/rover_pic" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>