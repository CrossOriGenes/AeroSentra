<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:background="@color/white"
    tools:context=".ProfileActivity">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:layout_marginHorizontal="10dp"
            android:layout_marginBottom="10dp">
            <!-- HEADER -->
            <include layout="@layout/profile_activity_header" />
            <!-- PROFILE IMAGE -->
            <include layout="@layout/profile_activity_image" />
            <!-- EDIT SECTION -->
            <include layout="@layout/profile_activity_edit_section" />
            <!-- SUBSCRIPTION SECTION -->
            <include layout="@layout/profile_activity_subscription_section" />
            <!-- SECURITY SECTION -->
            <include layout="@layout/profile_activity_securities_section" />
            <!-- MORE ACTIONS -->
            <include layout="@layout/profile_last_actions" />
        </LinearLayout>
    </ScrollView>

</RelativeLayout>